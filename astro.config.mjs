// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://iramsoto.github.io', // tu dominio de Pages de usuario
  base: '/NewPortfolio/',             // nombre EXACTO del repo
});
